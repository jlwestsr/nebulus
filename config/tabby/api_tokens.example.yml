# TabbyAPI Authentication Tokens
# Copy to api_tokens.yml and fill in real keys.
# Generate with: python3 -c "import secrets; print(secrets.token_hex(32))"
api_key: your_api_key_here
admin_key: your_admin_key_here
